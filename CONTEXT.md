# AIConnector

跨网段的两个 AI 通过共享通道交接实验任务和结果，实验执行由内侧 AI 或执行器负责。

## Language

**任务 Task**：需要完成的实验目标，包含代码基线、环境、入口、参数和验收标准。修改内容必须产生新的修订。

**修订 Revision**：任务内容的一次不可变版本。同一任务的不同修订不能混用结果。

**运行 Run**：针对某个修订的一次独立实验。重复实验需要新的运行 ID；重新读取同一消息不产生新运行。

**领取 Claim**：执行端对某次运行作出的持久化承诺。它表示这次运行已经交给本地执行者，不代表实验启动或完成已被远端观察到。

**结果 Result**：执行端提交的退出码、实际代码版本、结论、指标及产物清单。实验失败也可以产生完整结果。

**产物 Artifact**：任务或结果引用的文件，带稳定下载地址、字节数和 SHA-256。

**回执 Receipt**：接收方确认结果与产物完整收到的证据。它不替代实验验收标准的判断。

**通道 Channel**：两端共同访问的 Issue 评论与文件存储路径。
